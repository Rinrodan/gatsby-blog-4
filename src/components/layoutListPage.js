import * as React from 'react'
import { Link } from 'gatsby'
import {
  container,
  heading,
  navLinks,
  navButtons,
  navLinkItem,
  navLinkText 
} from './layoutListPage.module.css'

const LayoutListPage = ({ pageTitle, children }) => {
  return (
    <div className={container}>
      
  </div>
  )
}

export default LayoutListPage